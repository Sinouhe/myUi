import React from 'react';
import './style.scss';

const MixBlendButton = (): React.JSX.Element => {
  return (
    <button type="button" className="cyberBtn">
      {'cyber button'}
    </button>
  );
};

export default MixBlendButton;
